REST SERVER:
To run the REST server "node main.js"

To run the REST server you need MySQL installed and have a running database matching the .ENV credentials. Otherwise the server will fail.


PHP CONVERSION:
To run the converted php script "ts-node --esm phpconversion/javascriptversion.ts"